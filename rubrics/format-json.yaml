# JSON 格式规范 Rubric
# 使用方式：复制 assert 数组到项目 promptfooconfig.yaml 中内联使用；不要直接 $ref。
assert:
  - type: is-json
  - type: llm-rubric
    value: |
      请检查 AI 助手的回复是否符合以下 JSON 格式要求：
      1. 输出为合法的 JSON 格式
      2. 包含所有必要的字段
      3. 字段值类型正确（如字符串、数组、数字等）
      4. 无多余的字段或嵌套
      如果不符合，判定为 FAIL。
